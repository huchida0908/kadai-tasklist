<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li> task <%= link_to task.id, task %> : <%= task.content %> [ status : <%= task.status %> ] </li>
  <% end %>
</ul>

<%= link_to 'タスクの追加', new_task_path %>